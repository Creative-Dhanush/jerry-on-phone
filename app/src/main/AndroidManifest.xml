<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:label="Jerry Drill Wallpaper"
        android:icon="@mipmap/ic_launcher"
        android:theme="@android:style/Theme.Material.Light">

        <!-- Screen the user sees first: pick where Jerry should drill -->
        <activity android:name=".PositionPickerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- The actual live wallpaper engine -->
        <service
            android:name=".JerryDrillWallpaperService"
            android:label="Jerry Drill Wallpaper"
            android:permission="android.permission.BIND_WALLPAPER"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService"/>
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/wallpaper_info"/>
        </service>
    </application>
</manifest>
